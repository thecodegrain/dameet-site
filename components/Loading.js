import { useUser } from "@auth0/nextjs-auth0";

const Loading = () => {
  return (
    <>
      <h4 className="a-self text-center large-space">Loading...</h4>
    </>
  );
};

export default Loading;
